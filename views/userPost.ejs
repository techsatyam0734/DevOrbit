<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif }
    .line-clamp-2 {
      display: -webkit-box;
      -webkit-box-orient: vertical;
      overflow: hidden;
      line-clamp: 2;             
      -webkit-line-clamp: 2;     
    }
  </style>
</head>
<body class="bg-zinc-800 text-gray-100">
  <!-- Navbar -->
  <header class="bg-zinc-800 shadow-md sticky top-0 z-50">
    <div class="max-w-6xl mx-auto px-4 py-4 flex justify-between items-center">
      <h1 class="text-2xl font-extrabold text-blue-300 tracking-tight">
        DevOrbit
      </h1>
      <nav class="space-x-4">
        <a href="/" class="text-blue-300 hover:text-blue-500 transition">Home</a>
        <a href="/logout" class="bg-red-500 text-white hover:bg-red-600 rounded-md px-4 py-2 text-sm">
          Logout
        </a>
      </nav>
    </div>
  </header>

  <!-- Back Button -->
  <div class="max-w-6xl mx-auto px-4 py-4">
    <a href="/profile" class="inline-block text-white hover:text-blue-400">
      <svg class="w-8 h-8" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7" />
      </svg>
    </a>
  </div>

  <!-- Post Title -->
  <div class="text-center mb-6 px-4">
    <h2 class="text-4xl font-bold text-blue-300 break-words"><%= post.title %></h2>
  </div>

  <!-- Post Content -->
  <div class="max-w-3xl mx-auto px-6 sm:px-8 py-10 bg-zinc-700 rounded-md shadow-md">
    <p class="text-yellow-400 text-lg leading-relaxed whitespace-pre-line">
      <%= post.content %>
    </p>
  </div>

</body>
</html>
